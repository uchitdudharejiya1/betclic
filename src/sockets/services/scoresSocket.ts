// Placeholder for a dedicated live-scores socket service.
// In phase 1 the odds socket also handles score:update events; this file
// stays as a hook for future separation if the vendor exposes a distinct feed.
export const scoresSocket = null;
